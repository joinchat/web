const initialState = {
    error: "",
    login: false,
    user_type: "Guest",
    fetching: false,
    data: "",
    data_user: "",
};

export default function userState(state: any = initialState) {
    return state;
}